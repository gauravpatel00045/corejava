import java.util.Arrays;
import java.util.Collections;

/**
 * TODO stub is generated but developer or programmer need to add code as required.
 */
public class ArrayIndexDemo {

    public static void main(String[] args) {
        ArrayIndexDemo.displayArrayIndex();
    }

    /**
     * It displays the index of array element
     */
    static void displayArrayIndex() {
        int a[] = {10, 20, 30, 40, 50, 60, 70, 80, 90};//declaration and instantiation
        System.out.println("Array list: " + a.toString());
        System.out.println("Array size: " + a.length);
        //int arrayIndex = +Collections.singletonList(a).indexOf(40);
        // System.out.println("Index of 40 is: " + arrayIndex);
    }

}
